name: ci
on:
  push:
  pull_request:
permissions:
  contents: read
concurrency:
  # A superseded run tells nobody anything: it is testing a candidate that has
  # already been replaced, while consuming a runner the new candidate needs.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.25.x'
      - name: Verify formatting
        run: |
          # This package is vendored into nomad-testnet and Nomad-browser,
          # where a formatting difference shows up as spurious divergence
          # between the copy that ships and the copy that is developed.
          unformatted="$(gofmt -l .)"
          if test -n "$unformatted"; then
            printf 'unformatted Go files:\n%s\n' "$unformatted"
            gofmt -d .
            exit 1
          fi
      - name: Install the tools the capability gates need
        run: |
          # PROD-24's egress capture needs tcpdump. Installing it here rather
          # than hoping the image ships it means the gate's requirement is
          # stated in the workflow, and a runner image that drops it fails this
          # step instead of quietly turning the gate into a skip.
          sudo apt-get update -qq
          sudo apt-get install -y -qq tcpdump
      - name: Run the tests, requiring the capability gates to actually run
        env:
          # A skip is green. This says the capability is present here, so a
          # gate that cannot run fails rather than reporting what a pass
          # reports -- which is what the egress capture did on every CI run
          # until 8fa3a57, because the runner is not root.
          NOMAD_REQUIRE_CAPABILITY_GATES: '1'
        run: go test -race ./...
      - run: go vet ./...
      - name: Static analysis, failing closed
        run: |
          # staticcheck exits 0 when it cannot analyse a module, so "clean" and
          # "read nothing" are the same exit code. The script proves the tool is
          # analysing with a positive control before it trusts a clean run.
          ./scripts/run-staticcheck.sh
      - name: Verify the vulnerability gate still fails closed
        run: ./scripts/test-scan-vulnerabilities.sh
      - name: Scan dependencies for reachable vulnerabilities
        run: |
          # A vulnerability gate in one repository is a gate on one
          # repository. This ran only in Nomad-browser, leaving the
          # integration root and the cryptographic core unscanned.
          ./scripts/scan-vulnerabilities.sh
